services:
  memebrain:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=${PORT:-3000}
      - DATA_PATH=/app/data
      - HF_HOME=/app/data/hf_cache
    env_file:
      - .env
    restart: unless-stopped
